# Groq API key — get one free at https://console.groq.com
# Without it the investigation agent runs in rule-based fallback mode.
GROQ_API_KEY=

# LLM used by the LangGraph investigation agent
GROQ_MODEL=llama-3.1-8b-instant
